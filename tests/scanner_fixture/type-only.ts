import type { ElementD } from "./element-d.ts";

const html = (strings: TemplateStringsArray): TemplateStringsArray => strings;
export const slot: ElementD | null = null;
export const view = html`<cl-d></cl-d>`;
